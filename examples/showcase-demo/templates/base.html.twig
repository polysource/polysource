<!DOCTYPE html>
<html lang="en" data-bs-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    {# Turbo prefetches every visible link by default — would silently
       hit /logout after the home page renders and kill the session.
       Disable globally for the showcase. #}
    <meta name="turbo-prefetch" content="false">
    <title>{% block title %}ShopCo — Polysource Showcase{% endblock %}</title>
    {% block stylesheets %}{% endblock %}
    {% block javascripts %}
        {% block importmap %}{{ importmap('app') }}{% endblock %}
    {% endblock %}
</head>
<body>
    {% block body %}{% endblock %}
    {# Polysource Cmd+K search palette — Stimulus controller binds
       Cmd+K / Ctrl+K / "/" hotkeys globally. Rendered only when
       authenticated. #}
    {% if app.user is defined and app.user is not null %}
        {{ polysource_search_palette() }}
    {% endif %}
</body>
</html>
